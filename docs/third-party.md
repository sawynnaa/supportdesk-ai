# 第三方依赖与素材

运行时依赖版本以 pnpm-lock.yaml 为准。主要软件包括 Vue、Vite、Vue Router、Pinia、Element Plus、NestJS、Prisma、pg、PGlite、pgvector、bcryptjs、Zod、Marked、DOMPurify、Lucide。它们各自的 LICENSE 随 npm 包分发；本项目 MIT 不替代上游授权。

界面没有下载照片或商业素材；装饰由 CSS 绘制，图标来自 Lucide。字体使用系统回退栈，不依赖外部字体请求。虚构产品 FlowSpace 的文档、工单和人物由项目种子脚本生成，不含真实客户信息。
